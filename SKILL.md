# Open-H3-Context-IR (Ref2VA)

把用户的自由多模态输入（文字指令 + ≤9 图 + ≤3 视频 + ≤3 音频）改写为 H3-Base-Ref2VA 可直接消费的
Context Intermediate Representation（六段式，见 `references/ref_guide_en.md`）。

**本 skill 的立场：格式是记账，裁决是工作。** 官方 Context-IR 收费卖的不是六段式模板，
而是五种裁决能力。本文件的全部内容围绕这五种裁决组织；六段式只在最后一步作为裁决结果的登记表出现。
任何一步如果你发现自己在"填模板"而不是在"下判断"，你已经跑偏了。

五种裁决：

1. **引用裁决** — 每个素材的哪一束属性进入目标视频，哪些被明确抛弃
2. **冲突与超载仲裁** — 素材之间、素材与指令之间、内容与时长之间的矛盾，牺牲什么保什么
3. **时间轴叙事** — cut 落在哪一毫秒、每个镜头的戏剧功能是什么
4. **防伪影写作** — 用措辞替生成模型堵住可预见的失败模式
5. **克制补全** — 补质感不补情节，补到用户"看不出被补过"为止

对齐官方是唯一北极星：所有默认值、所有触发规则都必须能溯源到
`references/case_law.md` 中的官方判例，或标注为 `[HYPOTHESIS]` 等待
`alignment/ALIGN.md` 协议验证。**禁止发明没有判例支撑却不标注的规则。**

---

## 执行流程

严格按序执行 P0→P6。P0–P4 是裁决，产出写进工作草稿（不进最终输出）；P5 才开始写 IR 正文。
**P0–P4 的草稿必须保留**——它是与官方做裁决一致性对比（ALIGN.md §3）的原材料，也是蒸馏训练时的
思维链标签。只交终稿不留裁决记录的执行视为失败。

### P0 · 感知取证（调工具，不下判断）

对每个素材调用 `tools/TOOLS.md` 中的取证工具，得到**属性证据束**，不是 caption：

- 每个视频 → `shot_ledger`（精确切镜时间戳 + 每镜头帧）+ `visual_attributes`（逐镜头属性束）
  + `asset_probe`（时长/fps/分辨率/有无音轨）
- 每个图片 → `visual_attributes`（属性束 + 版式/文字/风格层单独列出）
- 每个音频（含视频自带音轨，若被指令涉及）→ `speech_verbatim`（词级时间戳逐字稿，
  原语言，听不清记 `[unclear]`）+ `voice_timbre` + `music_profile`（乐器/速度/结构/节拍网格，禁情绪词）
- 素材间 → `cross_asset_binder`（同一人/物/场景跨素材的同一性判定 + 置信度）

工具未补齐时的降级路径见各 stub 的 `fallback` 字段。**取证阶段禁止裁决**：不写"这个人应该保留"，
只写"图1含一名女性：外观束{...}、服装束{...}、姿态束{...}"。

### P1 · Intent Brief（读懂人，一段话 + 三件事）

写一段话回答：用户到底要什么成片、什么体裁、素材在他心里的轻重排序。
然后回答强制问题：

> **用户拿到成片后，最先检查的三件事是什么？**

这三件事是本次改写的保真红线。答不出来 = 意图没读懂 = 回去重读指令，不许往下走。
典型答案长这样（羊羔判例）：①还是原来那段视频吗（构图/光线/背景羊没变）②他说的是不是我给的那两句话
③声音像不像我给的音色。三件事之外的一切都是可协商的自由度。

**读意图的三个反直觉纪律：**

- **指令的沉默不是授权。** 用户没提的素材属性，默认处理方式查 case_law 的"欠定局面"判例，
  不查你的直觉。
- **否定意图优先级最高。**"镜头别动""不要说话"这类禁令必须在 P4 落成正面措辞
  （static shot / lips remain closed），禁令是最容易在 350 词长文里被冲掉的东西。
- **风格词必须物理化。**"高级感""氛围感""电影感"不许原样出现在 IR 里，必须在此处翻译成
  光线、色彩、运镜、节奏的物理参数（翻译对照表见 case_law §D，缺判例时标 `[HYPOTHESIS]`）。

### P2 · 引用裁决（对每束属性：cite / discard / transfer）

每个素材是一束可独立引用的属性，不是一个整体：

```
身份 | 外观 | 服装 | 道具 | 动作 | 姿态/表情 | 场景/环境 | 光线 | 运镜 | 剪辑节奏 | 风格 | 版式/文字 | 音色 | 词曲内容 | 节拍/律动 | 音效质感
```

对每束属性下三选一的裁决并写一行理由：

- `cite` → 它将成为一个 `<Subject N>` 的定义成分，或 `<Video N>`/`<Audio N>` 的引用维度
- `discard` → **必须显式写出**。全保等于过约束，是 ref2va 第一大失败模式
  （用户要图里的裙子，你把穿裙子的人也搬进去了）。每个素材至少检查一遍：
  "这里面有什么用户**不想要**？"
- `transfer` → 属性从来源迁到另一个可识别目标（A 的动作给 B 做）。transfer 必须写清
  受体是谁，后面 retention_analysis 记 `attribute_transfer`

裁决完成后才分配标签，标签是裁决的记账结果：

- 被 cite 的可见内容 → `<Subject N>`（一个 Subject 可由多素材共同定义，写清各素材贡献哪束属性）
- 图片仅当充当具体帧锚或分镜锚才单列 `<Picture N>`，只用来定义角色/场景/风格的图**不单列**，
  在 Subject 定义里引用来源即可
- 单列 `<Picture N>` 不等于 `keyframe completion`：只有图片必须成为目标视频某一具体帧时才用
  `keyframe completion`；仅提供构图、视点、主体位置或镜头顺序的 storyboard/shot-planning anchor
  仍用 `reference generation`（case_law B-7）
- `<Video N>` 只留给整片关系（被编辑/被续写/借时间结构）；视频里的人物场景被复用属于 Subject
- `<Audio N>` 按角色定义（复制信号 / 参考音色 / 参考曲风 / 复用词曲 / 参考节拍），
  一条音频多角色写成一句自然话，不拆多条

### P3 · 冲突与超载仲裁

逐条列出并写一行裁决 + 理由：

- **素材 vs 素材**：图1的光线与视频1的场景打架 → 谁让位？判据：P1 三件事里谁在场谁赢；
  都不在场 → 查 case_law 欠定判例；再无 → 服从"目标视频物理自洽"并标 `[HYPOTHESIS]`
- **素材 vs 指令**：指令说"她穿红裙"但图里是蓝裙 → 指令赢（文字是显式意图，素材是默认值），
  retention 记 `partially_preserved` 并写明改了什么
- **内容 vs 时长**：调 `speech_budget` 把全部台词折算成秒数（含 15–25% 的呼吸/停顿余量）。
  装不下 → 仲裁顺序：①加时长（≤15s 上限内）②砍最不服务 P1 三件事的句子③加语速（下策，
  仅在指令锁死时长时用）。砍词必须在草稿里记录砍了哪句、为什么是它
- **多主体绑定**：图1的脸 + 图2的衣服 + 视频1的场景合成一个人时，用 `cross_asset_binder`
  的同一性证据决定这是"一个 Subject 多来源定义"还是"attribute_transfer"。判据：用户语义里
  它们是不是同一个"角色"

### P4 · Beat Sheet（先有戏，再有字）

**跳过此步直接写长文，产物必然是流水账。** 节拍表回答四个问题：

1. **几个镜头，为什么。** 编辑类默认跟随源视频镜头结构（改结构需指令授权）；
   生成类按戏剧需要定：铺陈→揭示→反应→收束是最小完备弧线，5 秒单场景一镜到底也完全合法，
   镜头数服务于戏而不是丰富度
2. **每个 cut 落在哪一毫秒，踩的是什么。** cut 必须踩点：台词间隙 / 音乐节拍
   （`music_profile` 的节拍网格）/ 动作完成点 / 揭示时刻。写不出踩点理由的 cut 删掉。
   guide 的规矩：一个 cut 必须引入主体/空间/状态/视角/时间的新信息，只想改景别就用运镜不用 cut
3. **每个镜头的戏剧功能一句话。**"[Shot 2] 特写反应，承接 Shot 1 的能量积聚，白光爆发的承受者"
   ——写不出功能的镜头是凑数的
4. **时间预算表。** 每句 `<d>` 的起止秒（来自 speech_budget）、每个动作节拍的时长、
   结尾 settle 的留白（≥0.5s，动作要"落定"不能"戛止"，除非指令要 `<cutoff>`）

音频编排在此步一并裁决：源视频音轨去留（case_law A-3：编辑保留场景时 BGM 默认
`partially_copy` 压在新台词底下——这是官方一个没人要求也没人拒绝时的默认混音决策）、
新增台词与保留音乐的层次关系、canned laugh 之类音效事件挂在哪个节拍上。

### P5 · 扩写成 IR（裁决的登记，防伪影的战场）

按 `references/ref_guide_en.md` 的六段式登记 P1–P4 的全部裁决。格式细节全部服从 guide 原文，
此处只讲写作裁决：

- **detailed_description 的每一句话必须可渲染。** 逐句自查：这句话对应画面里/声音里的什么？
  内心活动、抽象评价、剧情解说一律物理化或删除。情绪只能以肌肉和动作出现
  （jaw clenches / shoulders tense / 手指停顿）
- **防伪影句式强制触发。** 查 `references/anti_artifact_phrasebook.md`，
  凡命中触发条件的句式必须写入，这不是文采是护栏。最低集：说完话必写嘴部闭合/下颌停止；
  画外音必写 lips remain completely closed；无台词的人物群像必写 silent；
  动作必须写到 settle 的终态；结尾一帧的状态必须钉死（钉死 = 写出结尾时每个主体的确切姿态与朝向，
  并 hold through the end）
- **细节密度对标官方样例，不对标词数。** 350–500 词是结果不是目标。每个镜头必须覆盖：
  当前构图、主体外观与帧内位置、环境与光线、动作与状态变化、运镜、当时的声音、
  引用标签在此处实际生效的方式。写成剧情梗概或引用关系清单 = 重写
- **首次出现即锚定。** 每个 `<Subject N>` 首次入画时把 cite 的属性束在可见范围内写实
  （长发/粉西装/怀中黑羔羊……），此后同标签复用不再重复定义
- **verbatim 纪律。** 复用的台词/歌词逐字进 `<d>`，原语言，`[unclear]` 不许猜；
  仅参考音色时**严禁**把参考音频里的词带进目标视频

### P6 · 收束审计（三道门，过不了就回炉）

1. **红线门**：P1 的三件事，逐件回答"它被钉死在第几镜、什么措辞保证它被渲染"。
   "提到了"不算钉死
2. **裁决门**：P2 每个 `discard` 确认没有从后门溜回正文；P3 每个仲裁结果确认落实；
   `[HYPOTHESIS]` 标注的规则若影响了关键裁决，在草稿里标记为高优先验证项
3. **格式门**：跑 `ir_linter`（约 30 条确定性断言，全部译自 guide），违规项回填自修一轮。
   linter 是仆人：它只保证登记无误，救不了裁决错误

---

## 失败模式清单（每次执行前读一遍）

按杀伤力排序：

1. **全保**：不敢 discard，把参考素材整体搬运 → P2 的显式 discard 是唯一解药
2. **流水账**：均匀切镜、无踩点、无功能 → 没做 P4
3. **梗概化**：detailed_description 写成剧情简介 + 引用关系列表 → 违反 P5 逐句可渲染
4. **时长塞爆**：台词装不进时间轴，生成时语速鬼畜或吞词 → 没跑 speech_budget
5. **音色污染**：参考音色时把参考音频的原词带进了目标 → P5 verbatim 纪律
6. **伪影裸奔**：说完话嘴还在动、画外音配了口型、结尾姿态漂移 → 没查 phrasebook
7. **过度补全**：用户给了产品图要展示视频，你给它编了个爱情故事 → 补质感不补情节，
   补全边界判例见 case_law §C
8. **发明规则**：拿不准时靠直觉定默认值且不标 `[HYPOTHESIS]` → 对齐官方无从谈起
9. **分镜冒充关键帧**：把 storyboard 图标成 `keyframe completion` → Ref2VA 不会因此获得像素级帧绑定，
   按 case_law B-7 改回 `reference generation`

---

## 文件地图

| 文件 | 角色 |
| --- | --- |
| `references/ref_guide_en.md` | 官方格式法条（形，一字未改） |
| `references/base_guide_en.md` | 官方 T2VA/I2VA/FL2VA/L2VA 基础法条（ref guide 的前置引用） |
| `references/case_law.md` | 官方判例逆向出的裁决规则（实，持续增补） |
| `references/anti_artifact_phrasebook.md` | 防伪影句式触发规则（实，从官方产物挖掘） |
| `tools/TOOLS.md` | 全部工具 stub 契约，供 agent 探索补齐 |
| `alignment/ALIGN.md` | 与官方对齐的采集、比对、复盘协议（北极星的操作化） |

## 工具调用路由

P0 不再把工具名当占位符。按素材实际存在的轨道调用：

```text
video -> asset_probe -> shot_ledger -> visual_attributes(逐镜头)
image -> asset_probe -> visual_attributes
audio/speech track -> asset_probe -> speech_verbatim -> voice_timbre
audio/music track -> asset_probe -> music_profile
cross-asset entities -> cross_asset_binder
dialogue list -> speech_budget
```

命令与模型配置见 `tools/TOOLS.md` 的“实现入口”。工具返回 `status=error` 或低置信字段时，只能执行该工具契约写明的 fallback；禁止用无标注直觉补齐。`speech_verbatim.status=needs_review` 时，人工完成 `[unclear]` 审核前，P2 不得 cite 任何词曲内容束。

P6 格式门调用 `python tools/ir_linter.py OUR_IR --duration TARGET_SECONDS`。T10 只修格式；出现裁决分歧仍按 `alignment/ALIGN.md` §3 归因。官方探针由 T9 采集，成片终审由 T11 生成三份盲评表；二者都不能跳过预测、盲跑与许可证闸门。
